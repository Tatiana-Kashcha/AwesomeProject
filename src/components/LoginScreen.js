import React from 'react';
import { StyleSheet, Text, View, TextInput, Button } from 'react-native';

export default function LoginScreen() {
  return (
    <View style={}>
<Text style={}>Увійти</Text>
<TextInput style={} placeholder='Адреса електронної пошти'/>
<TextInput style={} />
<Button style={} title='Увійти'/>
<Text style={}>Немає акаунту? Зареєструватися</Text>
    </View>
  );
}

const styles = StyleSheet.create({

});